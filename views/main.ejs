<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body style="display: flex; flex-direction: column; align-items: center; width:100%; background-color: purple; color: white;">
    <h1>
        Les paroles
    </h1>
    <form>
        <input type="text" name="guess" id="guess">
        <button type="submit">Essayer</button>
    </form>
    <div>
        <%paroles.split('\n').forEach(function(p){%>
            <p><%=p%></p>
        <%})%>

    </div>
</body>
</html>